

struct User{
    let id: String;
    let name: String;
    let username: String;
    let courses_as_student: Array;
    let courses_as_instructor: Array;
    let timeslots_as_students: Array;
    let timeslots_as_instructor: Array;
}